
import '../../App.css';  // To include the CSS for the spinner

const Loading = () => {
  return (
    <div className="loader">
      <div className="spinner"></div>
    </div>
  );
};

export default Loading;
